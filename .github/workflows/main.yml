name: Unit Testing

on:
  push:
    branches: [ dev2 ]


jobs:
  test_push_request:
    runs-on: ubuntu-latest
    steps:
      - name: Run Test Case
        uses: actions/checkout@v2
        with:
          # Full git history is needed to get a proper
          # list of changed files within `super-linter`
          fetch-depth: 0
      - uses: actions/setup-node@v1
        id: unitTest
        with:
          node-version: 16.19.0
      - run: npm ci
      - run: npm test
      - name: Make Request
        id: myRequest
        run: |
          curl --request POST \
          --url https://githubaction.atlassian.net/rest/api/2/issue/ \
          --header 'authorization: Bearer ATATT3xFfGF0w8bzQZKxmmWUOYWwriz5Xg-GT4hhY0nHwzOnly3XAbUyBjjqFWEEQ5aJjqA-4XqUN7-8KC1a3e81hMYJ9L8Oo4PglG-69c6tqrEI-4H5r9dpUeOXHi9_UvnqW72DwIil3xewwILN9pfUuBisUCj_NdLYw-mgLkPb1TwgW9dV-PY=E4128EAD' \
          --header 'content-type: application/json' \
          --data '{
                "fields": {
                  "project": {
                    "id": 10000
                  },
                  "summary": "Creating rest api github action.",
                  "description": "Creating of an issue using project keys and issue type names using the REST API",
                  "issuetype": {
                    "name": "Bug"
                  }
                }
              }' \
          --fail
     
    
     
        


       
        
